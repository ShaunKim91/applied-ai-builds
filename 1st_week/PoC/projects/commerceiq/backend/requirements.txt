# CommerceIQ backend dependencies
# torch is installed separately (CPU wheel by default, GPU wheel if nvidia-smi
# is detected) — see scripts/setup.sh and docker/Dockerfile for the exact command.

# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9

# --- Data / DB ---
sqlalchemy>=2.0,<3.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
pandas>=2.0,<3.0
numpy>=1.24,<2.1
pyarrow>=15.0
openpyxl>=3.1

# --- Auth ---
passlib[bcrypt]>=1.7,<2.0
bcrypt>=4.0,<4.1
python-jose[cryptography]>=3.3,<4.0
email-validator>=2.1

# --- AI / ML models (local, HuggingFace) ---
transformers>=4.40,<5.0
diffusers>=0.30,<1.0
accelerate>=0.30
safetensors>=0.4
sentence-transformers>=3.0,<4.0
statsmodels>=0.14,<1.0

# --- Vector store ---
chromadb>=0.5,<1.0

# --- HTTP client (OpenRouter etc.) ---
httpx>=0.27,<1.0
requests>=2.30,<3.0

# --- Misc ---
python-dotenv>=1.0
pillow>=10.0
