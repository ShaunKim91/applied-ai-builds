# Copy to .env and adjust for a real deployment. Safe demo defaults are
# baked into docker-compose.yml if this file is absent.
APP_PORT=8790
SECRET_KEY=threshold-dev-secret-change-in-production
ADMIN_EMAIL=admin@fenwickmutual.example
ADMIN_PASSWORD=ChangeMe123!
OPENROUTER_MODEL=qwen/qwen3-8b

# Real production deployment only (behind TLS): flip both, plus provide
# real SECRET_KEY/ADMIN_PASSWORD values above, or the app refuses to boot.
# ENFORCE_SECURE_DEFAULTS=true
# COOKIE_SECURE=true
